# `typescript-config`

This package contains the different (including a base) TypeScript configurations for the entire repository.

Extend the appropriate configuration in a workspace `tsconfig.json` file.

```"extends": "typescript-config/{config-name}.json"```
